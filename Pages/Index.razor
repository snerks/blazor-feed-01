@page "/"
@using System;
@using System.Threading.Tasks;

@inject HttpClient Http

@code {
    protected override async Task OnInitializedAsync()
    {
        //string url = "https://snerks.github.io/my-blog-starter/rss.xml";

        string url = "https://feeds.feedburner.com/GrabBagOfT";

        try
        {
            var rssXmlBytes = await Http.GetByteArrayAsync(url);
            Console.WriteLine("No exception");
        }
        catch (Exception ex)
        {
            Console.WriteLine(ex.ToString());
            throw;
        }
    }
}

